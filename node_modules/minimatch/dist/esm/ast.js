// parse a single path portion
import { parseClass } from './brace-expressions.js';
import { unescape } from './unescape.js';
const types = new Set(['!', '?', '+', '*', '@']);
const isExtglobType = (c) => types.has(c);
// Patterns that get prepended to bind to the start of either the
// entire string, or just a single path portion, to prevent dots
// and/or traversal patterns, when needed.
// Exts don't need the ^ or / bit, because the root binds that already.
const startNoTraversal = '(?!(?:^|/)\\.\\.?(?:$|/))';
const startNoDot = '(?!\\.)';
// characters that indicate a start of pattern needs the "no dots" bit,
// because a dot *might* be matched. ( is not in the list, because in
// the case of a child extglob, it will handle the prevention itself.
const addPatternStart = new Set(['[', '.']);
// cases where traversal is A-OK, no dot prevention needed
const justDots = new Set(['..', '.']);
const reSpecials = new Set('().*{}+?[]^$\\!');
const regExpEscape = (s) => s.replace(/[-[\]{}()*+?.,\\^$|#\s]/g, '\\$&');
// any single thing other than /
const qmark = '[^/]';
// * => any number of characters
const star = qmark + '*?';
// use + when we need to ensure that *something* matches, because the * is
// the only thing in the path portion.
const starNoEmpty = qmark + '+?';
// remove the \ chars that we added if we end up doing a nonmagic compare
// const deslash = (s: string) => s.replace(/\\(.)/g, '$1')
export class AST {
    type;
    #root;
    #hasMagic;
    #uflag = false;
    #parts = [];
    #parent;
    #parentIndex;
    #negs;
    #filledNegs = false;
    #options;
    #toString;
    // set to true if it's an extglob with no children
    // (which really means one child of '')
    #emptyExt = false;
    constructor(type, parent, options = {}) {
        this.type = type;
        // extglobs are inherently magical
        if (type)
            this.#hasMagic = true;
        this.#parent = parent;
        this.#root = this.#parent ? this.#parent.#root : this;
        this.#options = this.#root === this ? options : this.#root.#options;
        this.#negs = this.#root === this ? [] : this.#root.#negs;
        if (type === '!' && !this.#root.#filledNegs)
            this.#negs.push(this);
        this.#parentIndex = this.#parent ? this.#parent.#parts.length : 0;
    }
    get hasMagic() {
        /* c8 ignore start */
        if (this.#hasMagic !== undefined)
            return this.#hasMagic;
        /* c8 ignore stop */
        for (const p of this.#parts) {
            if (typeof p === 'string')
                continue;
            if (p.type || p.hasMagic)
                return (this.#hasMagic = true);
        }
        // note: will be undefined until we generate the regexp src and find out
        return this.#hasMagic;
    }
    // reconstructs the pattern
    toString() {
        if (this.#toString !== undefined)
            return this.#toString;
        if (!this.type) {
            return (this.#toString = this.#parts.map(p => String(p)).join(''));
        }
        else {
            return (this.#toString =
                this.type + '(' + this.#parts.map(p => String(p)).join('|') + ')');
        }
    }
    #fillNegs() {
        /* c8 ignore start */
        if (this !== this.#root)
            throw new Error('should only call on root');
        if (this.#filledNegs)
            return this;
        /* c8 ignore stop */
        // call toString() once to fill this out
        this.toString();
        this.#filledNegs = true;
        let n;
        while ((n = this.#negs.pop())) {
            if (n.type !== '!')
                continue;
            // walk up the tree, appending everthing that comes AFTER parentIndex
            let p = n;
            let pp = p.#parent;
            while (pp) {
                for (let i = p.#parentIndex + 1; !pp.type && i < pp.#parts.length; i++) {
                    for (const part of n.#parts) {
                        /* c8 ignore start */
                        if (typeof part === 'string') {
                            throw new Error('string part in extglob AST??');
                        }
                        /* c8 ignore stop */
                        part.copyIn(pp.#parts[i]);
                    }
                }
                p = pp;
                pp = p.#parent;
            }
        }
        return this;
    }
    push(...parts) {
        for (const p of parts) {
            if (p === '')
                continue;
            /* c8 ignore start */
            if (typeof p !== 'string' && !(p instanceof AST && p.#parent === this)) {
                throw new Error('invalid part: ' + p);
            }
            /* c8 ignore stop */
            this.#parts.push(p);
        }
    }
    toJSON() {
        const ret = this.type === null
            ? this.#parts.slice().map(p => (typeof p === 'string' ? p : p.toJSON()))
            : [this.type, ...this.#parts.map(p => p.toJSON())];
        if (this.isStart() && !this.type)
            ret.unshift([]);
        if (this.isEnd() &&
            (this === this.#root ||
                (this.#root.#filledNegs && this.#parent?.type === '!'))) {
            ret.push({});
        }
        return ret;
    }
    isStart() {
        if (this.#root === this)
            return true;
        // if (this.type) return !!this.#parent?.isStart()
        if (!this.#parent?.isStart())
            return false;
        if (this.#parentIndex === 0)
            return true;
        // if everything AHEAD of this is a negation, then it's still the "start"
        const p = this.#parent;
        for (let i = 0; i < this.#parentIndex; i++) {
            const pp = p.#parts[i];
            if (!(pp instanceof AST && pp.type === '!')) {
                return false;
            }
        }
        return true;
    }
    isEnd() {
        if (this.#root === this)
            return true;
        if (this.#parent?.type === '!')
            return true;
        if (!this.#parent?.isEnd())
            return false;
        if (!this.type)
            return this.#parent?.isEnd();
        // if not root, it'll always have a parent
        /* c8 ignore start */
        const pl = this.#parent ? this.#parent.#parts.length : 0;
        /* c8 ignore stop */
        return this.#parentIndex === pl - 1;
    }
    copyIn(part) {
        if (typeof part === 'string')
            this.push(part);
        else
            this.push(part.clone(this));
    }
    clone(parent) {
        const c = new AST(this.type, parent);
        for (const p of this.#parts) {
            c.copyIn(p);
        }
        return c;
    }
    static #parseAST(str, ast, pos, opt) {
        let escaping = false;
        let inBrace = false;
        let braceStart = -1;
        let braceNeg = false;
        if (ast.type === null) {
            // outside of a extglob, append until we find a start
            let i = pos;
            let acc = '';
            while (i < str.length) {
                const c = str.charAt(i++);
                // still accumulate escapes at this point, but we do ignore
                // starts that are escaped
                if (escaping || c === '\\') {
                    escaping = !escaping;
                    acc += c;
                    continue;
                }
                if (inBrace) {
                    if (i === braceStart + 1) {
                        if (c === '^' || c === '!') {
                            braceNeg = true;
                        }
                    }
                    else if (c === ']' && !(i === braceStart + 2 && braceNeg)) {
                        inBrace = false;
                    }
                    acc += c;
                    continue;
                }
                else if (c === '[') {
                    inBrace = true;
                    braceStart = i;
                    braceNeg = false;
                    acc += c;
                    continue;
                }
                if (!opt.noext && isExtglobType(c) && str.charAt(i) === '(') {
                    ast.push(acc);
                    acc = '';
                    const ext = new AST(c, ast);
                    i = AST.#parseAST(str, ext, i, opt);
                    ast.push(ext);
                    continue;
                }
                acc += c;
            }
            ast.push(acc);
            return i;
        }
        // some kind of extglob, pos is at the (
        // find the next | or )
        let i = pos + 1;
        let part = new AST(null, ast);
        const parts = [];
        let acc = '';
        while (i < str.length) {
            const c = str.charAt(i++);
            // still accumulate escapes at this point, but we do ignore
            // starts that are escaped
            if (escaping || c === '\\') {
                escaping = !escaping;
                acc += c;
                continue;
            }
            if (inBrace) {
                if (i === braceStart + 1) {
                    if (c === '^' || c === '!') {
                        braceNeg = true;
                    }
                }
                else if (c === ']' && !(i === braceStart + 2 && braceNeg)) {
                    inBrace = false;
                }
                acc += c;
                continue;
            }
            else if (c === '[') {
                inBrace = true;
                braceStart = i;
                braceNeg = false;
                acc += c;
                continue;
            }
            if (isExtglobType(c) && str.charAt(i) === '(') {
                part.push(acc);
                acc = '';
                const ext = new AST(c, part);
                part.push(ext);
                i = AST.#parseAST(str, ext, i, opt);
                continue;
            }
            if (c === '|') {
                part.push(acc);
                acc = '';
                parts.push(part);
                part = new AST(null, ast);
                continue;
            }
            if (c === ')') {
                if (acc === '' && ast.#parts.length === 0) {
                    ast.#emptyExt = true;
                }
                part.push(acc);
                acc = '';
                ast.push(...parts, part);
                return i;
            }
            acc += c;
        }
        // unfinished extglob
        // if we got here, it was a malformed extglob! not an extglob, but
        // maybe something else in there.
        ast.type = null;
        ast.#hasMagic = undefined;
        ast.#parts = [str.substring(pos - 1)];
        return i;
    }
    static fromGlob(pattern, options = {}) {
        const ast = new AST(null, undefined, options);
        AST.#parseAST(pattern, ast, 0, options);
        return ast;
    }
    // returns the regular expression if there's magic, or the unescaped
    // string if not.
    toMMPattern() {
        // should only be called on root
        /* c8 ignore start */
        if (this !== this.#root)
            return this.#root.toMMPattern();
        /* c8 ignore stop */
        const glob = this.toString();
        const [re, body, hasMagic, uflag] = this.toRegExpSource();
        // if we're in nocase mode, and not nocaseMagicOnly, then we do
        // still need a regular expression if we have to case-insensitively
        // match capital/lowercase characters.
        const anyMagic = hasMagic ||
            this.#hasMagic ||
            (this.#options.nocase &&
                !this.#options.nocaseMagicOnly &&
                glob.toUpperCase() !== glob.toLowerCase());
        if (!anyMagic) {
            return body;
        }
        const flags = (this.#options.nocase ? 'i' : '') + (uflag ? 'u' : '');
        return Object.assign(new RegExp(`^${re}$`, flags), {
            _src: re,
            _glob: glob,
        });
    }
    get options() {
        return this.#options;
    }
    // returns the string match, the regexp source, whether there's magic
    // in the regexp (so a regular expression is required) and whether or
    // not the uflag is needed for the regular expression (for posix classes)
    // TODO: instead of injecting the start/end at this point, just return
    // the BODY of the regexp, along with the start/end portions suitable
    // for binding the start/end in either a joined full-path makeRe context
    // (where we bind to (^|/), or a standalone matchPart context (where
    // we bind to ^, and not /).  Otherwise slashes get duped!
    //
    // In part-matching mode, the start is:
    // - if not isStart: nothing
    // - if traversal possible, but not allowed: ^(?!\.\.?$)
    // - if dots allowed or not possible: ^
    // - if dots possible and not allowed: ^(?!\.)
    // end is:
    // - if not isEnd(): nothing
    // - else: $
    //
    // In full-path matching mode, we put the slash at the START of the
    // pattern, so start is:
    // - if first pattern: same as part-matching mode
    // - if not isStart(): nothing
    // - if traversal possible, but not allowed: /(?!\.\.?(?:$|/))
    // - if dots allowed or not possible: /
    // - if dots possible and not allowed: /(?!\.)
    // end is:
    // - if last pattern, same as part-matching mode
    // - else nothing
    //
    // Always put the (?:$|/) on negated tails, though, because that has to be
    // there to bind the end of the negated pattern portion, and it's easier to
    // just stick it in now rather than try to inject it later in the middle of
    // the pattern.
    //
    // We can just always return the same end, and leave it up to the caller
    // to know whether it's going to be used joined or in parts.
    // And, if the start is adjusted slightly, can do the same there:
    // - if not isStart: nothing
    // - if traversal possible, but not allowed: (?:/|^)(?!\.\.?$)
    // - if dots allowed or not possible: (?:/|^)
    // - if dots possible and not allowed: (?:/|^)(?!\.)
    //
    // But it's better to have a simpler binding without a conditional, for
    // performance, so probably better to return both start options.
    //
    // Then the caller just ignores the end if it's not the first pattern,
    // and the start always gets applied.
    //
    // But that's always going to be $ if it's the ending pattern, or nothing,
    // so the caller can just attach $ at the end of the pattern when building.
    //
    // So the todo is:
    // - better detect what kind of start is needed
    // - return both flavors of starting pattern
    // - attach $ at the end of the pattern when creating the actual RegExp
    //
    // Ah, but wait, no, that all only applies to the root when the first pattern
    // is not an extglob. If the first pattern IS an extglob, then we need all
    // that dot prevention biz to live in the extglob portions, because eg
    // +(*|.x*) can match .xy but not .yx.
    //
    // So, return the two flavors if it's #root and the first child is not an
    // AST, otherwise leave it to the child AST to handle it, and there,
    // use the (?:^|/) style of start binding.
    //
    // Even simplified further:
    // - Since the start for a join is eg /(?!\.) and the start for a part
    // is ^(?!\.), we can just prepend (?!\.) to the pattern (either root
    // or start or whatever) and prepend ^ or / at the Regexp construction.
    toRegExpSource(allowDot) {
        const dot = allowDot ?? !!this.#options.dot;
        if (this.#root === this)
            this.#fillNegs();
        if (!this.type) {
            const noEmpty = this.isStart() && this.isEnd();
            const src = this.#parts
                .map(p => {
                const [re, _, hasMagic, uflag] = typeof p === 'string'
                    ? AST.#parseGlob(p, this.#hasMagic, noEmpty)
                    : p.toRegExpSource(allowDot);
                this.#hasMagic = this.#hasMagic || hasMagic;
                this.#uflag = this.#uflag || uflag;
                return re;
            })
                .join('');
            let start = '';
            if (this.isStart()) {
                if (typeof this.#parts[0] === 'string') {
                    // this is the string that will match the start of the pattern,
                    // so we need to protect against dots and such.
                    // '.' and '..' cannot match unless the pattern is that exactly,, SHIFT tugmasini bosib turing.
        
      Klaviatura aylantirish boshqaruvi. Modelingiz atrofida aylantirish uchun ALT tugmasini bosib turgan holda strelka tugmalarini bosing.
       MR@Scale Ichki foydalanuvchilari, xush kelibsiz Maketni barcha burchaklardan ko‘rib chiqish   Sizga yoqqan yoki yoqmagan yana biror narsa bormi?  Yuklash sozlamalari Maketni yuklab bo‘lmadi   To‘liq ekran ko‘rinishi Qandaydir kutilmagan xato yuz berdi Immersiv rejimda kutilmagan xato yuz berdi, ilova yopiladi. Iltimos, qayta ishga tushiring. Immersiv rejim tayyor   Maketni immersiv tajribada ko‘rish uchun endi qurilmangizni taqishingiz mumkin.   Maket Immersiv rejimda ochilmoqda   Maketni immersiv rejimga yuklab bo‘lmadi. Uni ishchi stol rejimiga qayta yuklaganimizdan so‘ng, qayta urinib ko‘ring. Immersiv rejimni ishga tushirib bo‘lmadi  Afsuski, Immersiv rejimni ishga tushirib bo‘lmadi.    Immersiv rejimdan chiqish   Past FPS aniqlandi  Biz tajribangiz juda past kadr tezligida bajarilayotganligini aniqladik. Tajribangiz yopildi.   Boshga mahkamlangan displey uzildi  Immersiv rejimdan chiqishda boshga o‘rnatilgan displey endi aniqlanmaydi. Immersiv rejimi o‘chirilmoqda Immersiv rejim  Buni Aralash reallik portaliga aylantiring  3D modellarni toping va ulardan foydalaning!    Immersiv rejimi Quloqlikni talab qiladi O‘tkazib yuborish Immersiv rejimni ishga tushirish uchun Quloqlikni ulang va Windows Aralash reallik Portali initsializatsiya qilinganiga ishonch hosil qiling.   Panellarni berkitish    Berkitilgan Yordam  Katak koʻrinishi   Interval    Chiziqlar orasidagi interval    Katak va koʻrinishlar  Yarqirash/Notekislik    Fikr-mulohaza markazini ishga tushirish Fikr-mulohaza yuboring  3D model bilan aloqa qilish Ishga tushiring!    Butun ekran rejimi va menyu paneli  To‘liq ekranga o‘tish   To‘liq ekran  Old Nuqtali rasm    Tanlangan maketni topa olmadik. 3D maketni yuklab bo‘lmadi    {0}’ni ochib bo‘lmadi.  Koʻrish maydoni    Suratga olish sifati haqida fikr-mulohaza   G‘oya yoki funksiya talabini yuborish Funksiya taklif qilish  Biror narsa ishlamasa, bizga xabar bering   Muammo haqida xabar berish  Fikr-mulohaza   Yangilash   Yangilash talab etiladi Bu modelni ochish uchun 3D rejimda ko‘rish vositasini Microsoft Store orqali yangilang.   Ko‘rib chiqish vositasini Immersiv rejim uchun tayyorlab bo‘lmadi   Afsuski, ko‘rib chiqish vositasidan maketni chiqarib tashlab bo‘lmadi, avval uni Immersiv rejimda yuklashga urinamiz.   Maketni Ko‘rib chiqish vositasiga qayta yuklab bo‘lmadi Afsuski, maketni ko‘rib chiqish vositasiga qayta yuklay olmadik, iltimos, ko‘rib chiqish vositasini qayta ishga tushiring.  3D chop etib bo‘lmadi Kechirasiz, bu maketni 3D chop eta olmadik, chunki unda to‘rlar yo‘q.   Minecraft asarlarini 3D chop etib bo‘lmaydi   3D chop etish tajribamizni yaxshilash bilan bandmiz. Tez orada yana qayta tekshiring.   Kechirasiz, ushbu maketni 3D chop eta olmadik.  Kechirasiz, fayl turi hozirda qo‘llab-quvvatlanmaydi. Masshtab: {0:P0}    Eni O‘lcham va aniqlik darajasi   Tur sifatida saqlash    Soyalarni renderlash    Professional maslahat: animatsiyani pauza qiling va rasm sifatida eksport qilish uchun kerakli ramkani skrubber bilan tanlang   Razm solish Mutanosiblikni mahkamkash   Rasmlar ketma-ketligi   Sifati: {0:P0} (juda past)  Sifati {0:P0} (juda yuqori) Sifati: {0:P0} (o‘rta)    Sifati: {0:P0} (past)   Sifati: {0:P0} (yuqori) Balandlik   Vaqtincha xotiraga nusxalash    Rasmni eksport qilish   Sozlamalarni saqlash    JPEG tasvir sifati  Eksport qilinmadi   Bu rasm eksport qilinmadi.  Chiqish Ekskluziv butun ekran rejimi    Muhit   Muhit va yoritish paneli ochiq  Muhit va yoritish   Kamerada aksini ko‘rsatish    Aralash reallik ko‘rinishida akslantirilgan rasmni ko‘rsatish   Emissiv Ajratuvchi rang Tahrirlash  Chizish so‘rovlari    Maket yuklab olinmoqda  Bu modelda ko‘proq amal bajarish uchun kengaytiring   3D bilan ko‘proq  3D rejimda ko‘rish vositasi mikrofonni ishlatish yoki ishlatmasligini belgilash   3D rejimda ko‘rish vositasi joylashuv axborotini olish yoki olmasligini belgilash 3D rejimda ko‘rish vositasi kameraga kirish yoki kira olmasligini belgilash   Qorong‘i  Paint 3D’ni ocha olmadik. Bir oz kuting, so‘ngra qayta urinib ko‘ring.    Buferga nusxa olindi    © 2017 Microsoft korporatsiyasi    Nusxalash   Maketingiz yuklanmoqda  Tushunarli  Yana boshqa onlayn modellarni olish 3D kutubxonani ochish   Onlayn modellar to‘plami bilan tanishing. Ushbu salomlashish oynasini yoping  Boshqaruv elementlarini yopish  Ushbu salomlashish ekranini yopish  Yaqinda ochilgan fayllar ro‘yxatini tozalash  Barcha funksiyalardan belgilash olib tashlandi  Erkin   Voy!    Rasm yoki videoga olayotganda muammo yuz berdi. To‘xtatish    Taymer o‘chirilgan    Taymer 7 soniya Taymer 2 soniya Taymer 10 soniya    Avval kamerangizni sozlang  Kamera ulanganligini tekshiring Kechirasiz, kamerangiz bilan bog‘liq muammoga duch keldik. Ulanganligi va foydalanishga tayyorligini tekshiring.  Kameradan foydalanib bo‘lmaydi    Kamerani ishga tushirishda muammo yuzaga keldi. U boshqa ilova tomonidan foydalanayotgan bo‘lishi mumkin. Kameradan foydalanayotgan ilovalarni yoping va qayta urinib ko‘ring.  Sozlamalarda kameradan foydalanishga ruxsat bering  3D rejimda ko‘rish vositasi qurilmangiz kamerasidan foydalanish ruxsatini talab qiladi.   Past    Suyaklar    Asosiy rang Automatik qator orasi intervali Hisob qaydnomasini tanlang  Fotosuratlar qo‘shing (ixtiyoriy) Ilova   3D rejimda ko‘rish vositasi razm solish   Yangi 3D rejimda ko‘rish vositasi ommaviy chiqarilishidan oldin uning yangi funksiyalaridan foydalaning!  Ilovalarni almashtirmoqchimidingiz? “3D rejimda ko‘rish vositasi” Sozlamalarni ochishga urinmoqda Animatsiya davomiyligi  1,5 marta tezroq animatsiya tezligi Normal animatmatsiya tezligi    Yarim animatsiya tezligi    {0:N1} karra animatsiya tezligi 2 marta tezroq animatsiya tezligi   Animatsiya tezligi  Maket animatsiyalari    Bu model bilan birga kelgan animatsiya uslullari    Animatsiya {0:N0}   Ayni vaqtda Paint 3D animatsiyani qo‘llab-quvvatlamaydi   Bu maketni Paint 3D�