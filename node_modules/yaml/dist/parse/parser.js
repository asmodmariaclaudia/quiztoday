'use strict';

var cst = require('./cst.js');
var lexer = require('./lexer.js');

function includesToken(list, type) {
    for (let i = 0; i < list.length; ++i)
        if (list[i].type === type)
            return true;
    return false;
}
function findNonEmptyIndex(list) {
    for (let i = 0; i < list.length; ++i) {
        switch (list[i].type) {
            case 'space':
            case 'comment':
            case 'newline':
                break;
            default:
                return i;
        }
    }
    return -1;
}
function isFlowToken(token) {
    switch (token?.type) {
        case 'alias':
        case 'scalar':
        case 'single-quoted-scalar':
        case 'double-quoted-scalar':
        case 'flow-collection':
            return true;
        default:
            return false;
    }
}
function getPrevProps(parent) {
    switch (parent.type) {
        case 'document':
            return parent.start;
        case 'block-map': {
            const it = parent.items[parent.items.length - 1];
            return it.sep ?? it.start;
        }
        case 'block-seq':
            return parent.items[parent.items.length - 1].start;
        /* istanbul ignore next should not happen */
        default:
            return [];
    }
}
/** Note: May modify input array */
function getFirstKeyStartProps(prev) {
    if (prev.length === 0)
        return [];
    let i = prev.length;
    loop: while (--i >= 0) {
        switch (prev[i].type) {
            case 'doc-start':
            case 'explicit-key-ind':
            case 'map-value-ind':
            case 'seq-item-ind':
            case 'newline':
                break loop;
        }
    }
    while (prev[++i]?.type === 'space') {
        /* loop */
    }
    return prev.splice(i, prev.length);
}
function fixFlowSeqItems(fc) {
    if (fc.start.type === 'flow-seq-start') {
        for (const it of fc.items) {
            if (it.sep &&
                !it.value &&
                !includesToken(it.start, 'explicit-key-ind') &&
                !includesToken(it.sep, 'map-value-ind')) {
                if (it.key)
                    it.value = it.key;
                delete it.key;
                if (isFlowToken(it.value)) {
                    if (it.value.end)
                        Array.prototype.push.apply(it.value.end, it.sep);
                    else
                        it.value.end = it.sep;
                }
                else
                    Array.prototype.push.apply(it.start, it.sep);
                delete it.sep;
            }
        }
    }
}
/**
 * A YAML concrete syntax tree (CST) parser
 *
 * ```ts
 * const src: string = ...
 * for (const token of new Parser().parse(src)) {
 *   // token: Token
 * }
 * ```
 *
 * To use the parser with a user-provided lexer:
 *
 * ```ts
 * function* parse(source: string, lexer: Lexer) {
 *   const parser = new Parser()
 *   for (const lexeme of lexer.lex(source))
 *     yield* parser.next(lexeme)
 *   yield* parser.end()
 * }
 *
 * const src: string = ...
 * const lexer = new Lexer()
 * for (const token of parse(src, lexer)) {
 *   // token: Token
 * }
 * ```
 */
class Parser {
    /**
     * @param onNewLine - If defined, called separately with the start position of
     *   each new line (in `parse()`, including the start of input).
     */
    constructor(onNewLine) {
        /** If true, space and sequence indicators count as indentation */
        this.atNewLine = true;
        /** If true, next token is a scalar value */
        this.atScalar = false;
        /** Current indentation level */
        this.indent = 0;
        /** Current offset since the start of parsing */
        this.offset = 0;
        /** On the same line with a block map key */
        this.onKeyLine = false;
        /** Top indicates the node that's currently being built */
        this.stack = [];
        /** The source of the current token, set in parse() */
        this.source = '';
        /** The type of the current token, set in parse() */
        this.type = '';
        // Must be defined after `next()`
        this.lexer = new lexer.Lexer();
        this.onNewLine = onNewLine;
    }
    /**
     * Parse `source` as a YAML stream.
     * If `incomplete`, a part of the last line may be left as a buffer for the next call.
     *
     * Errors are not thrown, but yielded as `{ type: 'error', message }` tokens.
     *
     * @returns A generator of tokens representing each directive, document, and other structure.
     */
    *parse(source, incomplete = false) {
        if (this.onNewLine && this.offset === 0)
            this.onNewLine(0);
        for (const lexeme of this.lexer.lex(source, incomplete))
            yield* this.next(lexeme);
        if (!incomplete)
            yield* this.end();
    }
    /**
     * Advance the parser by the `source` of one lexical token.
     */
    *next(source) {
        this.source = source;
        if (process.env.LOG_TOKENS)
            console.log('|', cst.prettyToken(source));
        if (this.atScalar) {
            this.atScalar = false;
            yield* this.step();
            this.offset += source.length;
            return;
        }
        const type = cst.tokenType(source);
        if (!type) {
            const message = `Not a YAML token: ${source}`;
            yield* this.pop({ type: 'error', offset: this.offset, message, source });
            this.offset += source.length;
        }
        else if (type === 'scalar') {
            this.atNewLine = false;
            this.atScalar = true;
            this.type = 'scalar';
        }
        else {
            this.type = type;
            yield* this.step();
            switch (type) {
                case 'newline':
                    this.atNewLine = true;
                    this.indent = 0;
                    if (this.onNewLine)
                        this.onNewLine(this.offset + source.length);
                    break;
                case 'space':
                    if (this.atNewLine && source[0] === ' ')
                        this.indent += source.length;
                    break;
                case 'explicit-key-ind':
                case 'map-value-ind':
                case 'seq-item-ind':
                    if (this.atNewLine)
                        this.indent += source.length;
                    break;
                case 'doc-mode':
                case 'flow-error-end':
                    return;
                default:
                    this.atNewLine = false;
            }
            this.offset += source.length;
        }
    }
    /** Call at end of input to push out any remaining constructions */
    *end() {
        while (this.stack.length > 0)
            yield* this.pop();
    }
    get sourceToken() {
        const st = {
            type: this.type,
            offset: this.offset,
            indent: this.indent,
            source: this.source
        };
        return st;
    }
    *step() {
        const top = this.peek(1);
        if (this.type === 'doc-end' && (!top || top.type !== 'doc-end')) {
            while (this.stack.length > 0)
                yield* this.pop();
            this.stack.push({
                type: 'doc-end',
                offset: this.offset,
                source: this.source
            });
            return;
        }
        if (!top)
            return yield* this.stream();
        switch (top.type) {
            case 'document':
                return yield* this.document(top);
            case 'alias':
            case 'scalar':
            case 'single-quoted-scalar':
            case 'double-quoted-scalar':
                return yield* this.scalar(top);
            case 'block-scalar':
                return yield* this.blockScalar(top);
            case 'block-map':
                return yield* this.blockMap(top);
            case 'block-seq':
                return yield* this.blockSequence(top);
            case 'flow-collection':
                return yield* this.flowCollection(top);
            case 'doc-end':
                return yield* this.documentEnd(top);
        }
        /* istanbul ignore next should not happen */
        yield* this.pop();
    }
    peek(n) {
        return this.stack[this.stack.length - n];
    }
    *pop(error) {
        const token = error ?? this.stack.pop();
        /* istanbul ignore if should not happen */
        if (!token) {
            const message = 'Tried to pop an empty stack';
            yield { type: 'error', offset: this.offset, source: '', message };
        }
        else if (this.stack.length === 0) {
            yield token;
        }
        else {
            const top = this.peek(1);
            if (token.type === 'block-scalar') {
                // Block scalars use their parent rather than header indent
                token.indent = 'indent' in top ? top.indent : 0;
            }
            else if (token.type === 'flow-collection' && top.type === 'document') {
                // Ignore all indent for top-level flow collections
                token.indent = 0;
            }
            if (token.type === 'flow-collection')
                fixFlowSeqItems(token);
            switch (top.type) {
                case 'document':
                    top.value = token;
                    break;
                case 'block-scalar':
                    top.props.push(token); // error
                    break;
                case 'block-map': {
                    const it = top.items[top.items.length - 1];
                    if (it.value) {
                        top.items.push({ start: [], key: token, sep: [] });
                        this.onKeyLine = true;
                        return;
                    }
                    else if (it.sep) {
                        it.value = token;
                    }
                    else {
                        Object.assign(it, { key: token, sep: [] });
                        this.onKeyLine = !includesToken(it.start, 'explicit-key-ind');
                        return;
                    }
                    break;
                }
                case 'block-seq': {
                    const it = top.items[top.items.length - 1];
                    if (it.value)
                        top.items.push({ start: [], value: token });
                    else
                        it.value = token;
                    break;
                }
                case 'flow-collection': {
                    const it = top.items[top.items.length - 1];
                    if (!it || it.value)
                        top.items.push({ start: [], key: token, sep: [] });
                    else if (it.sep)
                        it.value = token;
                    else
                        Object.assign(it, { key: token, sep: [] });
                    return;
                }
                /* istanbul ignore next should not happen */
                default:
                    yield* this.pop();
                    yield* this.pop(token);
            }
            if ((top.type === 'document' ||
                top.type === 'block-map' ||
                top.type === 'block-seq') &&
                (token.type === 'block-map' || token.type === 'block-seq')) {
                const last = token.items[token.items.length - 1];
                if (last &&
                    !last.sep &&
                    !last.value &&
                    last.start.length > 0 &&
                    findNonEmptyIndex(last.start) === -1 &&
                    (token.indent === 0 ||
                        last.start.every(st => st.type !== 'comment' || st.indent < token.indent))) {
                    if (top.type === 'document')
                        top.end = last.start;
                    else
                        top.items.push({ start: last.start });
                    token.items.splice(-1, 1);
                }
            }
        }
    }
    *stream() {
        switch (this.type) {
            case 'directive-line':
                yield { type: 'directive', offset: this.offset, source: this.source };
                return;
            case 'byte-order-mark':
            case 'space':
            case 'comment':
            case 'newline':
                yield this.sourceToken;
                return;
            case 'doc-mode':
            case 'doc-start': {
                const doc = {
                    type: 'document',
                    offset: this.offset,
                    start: []
                };
                if (this.type === 'doc-start')
                    doc.start.push(this.sourceToken);
                this.stack.push(doc);
                return;
            }
        }
        yield {
            type: 'error',
            offset: this.offset,
            message: `Unexpected ${this.type} token in YAML stream`,
            source: this.source
        };
    }
    *document(doc) {
        if (doc.value)
            return yield* this.lineEnd(doc);
        switch (this.type) {
            case 'doc-start': {
                if (findNonEmptyIndex(doc.start) !== -1) {
                    yield* this.pop();
                    yield* this.step();
                }
                else
                    doc.start.push(this.sourceToken);
                return;
            }
            case 'anchor':
            case 'tag':
            case 'space':
            case 'comment':
            case 'newline':
                doc.start.push(this.sourceToken);
                return;
        }
        const bv = this.startBlockValue(doc);
        if (bv)
            this.stack.push(bv);
        else {
            yield {
                type: 'error',
                offset: this.offset,
                message: `Unexpected ${this.type} token in YAML document`,
                source: this.source
            };
        }
    }
    *scalar(scalar) {
        if (this.type === 'map-value-ind') {
            const prev = getPrevProps(this.peek(2));
            const start = getFirstKeyStartProps(prev);
            let sep;
            if (scalar.end) {
                sep = scalar.end;
                sep.push(this.sourceToken);
                delete scalar.end;
            }
            else
                sep = [this.sourceToken];
            const map = {
                type: 'block-map',
                offset: scalar.offset,
                indent: scalar.indent,
                items: [{ start, key: scalar, sep }]
            };
            this.onKeyLine = true;
            this.stack[this.stack.length - 1] = map;
        }
        else
            yield* this.lineEnd(scalar);
    }
    *blockScalar(scalar) {
        switch (this.type) {
            case 'space':
            case 'comment':
            case 'newline':
                scalar.props.push(this.sourceToken);
                return;
            case 'scalar':
                scalar.source = this.source;
                // block-scalar source includes trailing newline
                this.atNewLine = true;
                this.indent = 0;
                if (this.onNewLine) {
                    let nl = this.source.indexOf('\n') + 1;
                    while (nl !== 0) {
                        this.onNewLine(this.offset + nl);
                        nl = this.source.indexOf('\n', nl) + 1;
                    }
                }
                yield* this.pop();
                break;
            /* istanbul ignore next should not happen */
            default:
                yield* this.pop();
                yield* this.step();
        }
    }
    *blockMap(map) {
        const it = map.items[map.items.length - 1];
        // it.sep is true-ish if pair already has key or : separator
        switch (this.type) {
            case 'newline':
                this.onKeyLine = false;
                if (it.value) {
                    const end = 'end' in it.value ? it.value.end : undefined;
                    const last = Array.isArray(end) ? end[end.length - 1] : undefined;
                    if (last?.type === 'comment')
                        end?.push(this.sourceToken);
                    else
                        map.items.push({ start: [this.sourceToken] });
                }
                else if (it.sep) {
                    it.sep.push(this.sourceToken);
                }
                else {
                    it.start.push(this.sourceToken);
                }
                return;
            case 'space':
            case 'comment':
                if (it.value) {
                    map.items.push({ start: [this.sourceToken] });
                }
                else if (it.sep) {
                    it.sep.push(this.sourceToken);
                }
                else {
                    if (this.atIndentedComment(it.start, map.indent)) {
                        const prev = map.items[map.items.length - 2];
                        const end = prev?.value?.end;
                        if (Array.isArray(end)) {
                            Array.prototype.push.apply(end, it.start);
                            end.push(this.sourceToken);
                            map.items.pop();
                            return;
                        }
                    }
                    it.start.push(this.sourceToken);
                }
                return;
        }
        if (this.indent >= map.indent) {
            const atNextItem = !this.onKeyLine &&
                this.indent === map.indent &&
                it.sep &&
                this.type !== 'seq-item-ind';
            // For empty nodes, assign newline-separated not indented empty tokens to following node
            let start = [];
            if (atNextItem && it.sep && !it.value) {
                const nl = [];
                for (let i = 0; i < it.sep.length; ++i) {
                    const st = it.sep[i];
                    switch (st.type) {
                        case 'newline':
                            nl.push(i);
                            break;
                        case 'space':
                            break;
                        case 'comment':
                            if (st.indent > map.indent)
                                nl.length = 0;
                            break;
                        default:
                            nl.length = 0;
                    }
                }
                if (nl.length >= 2)
                    start = it.sep.splice(nl[1]);
            }
            switch (this.type) {
                case 'anchor':
                case 'tag':
                    if (atNextItem || it.value) {
                        start.push(this.sourceToken);
                        map.items.push({ start });
                        this.onKeyLine = true;
                    }
                    else if (it.sep) {
                        it.sep.push(this.sourceToken);
                    }
                    else {
                        it.start.push(this.sourceToken);
                    }
                    return;
                case 'explicit-key-ind':
                    if (!it.sep && !includesToken(it.start, 'explicit-key-ind')) {
                        it.start.push(this.sourceToken);
                    }
                    else if (atNextItem || it.value) {
                        start.push(this.sourceToken);
                        map.items.push({ start });
                    }
                    else {
                        this.stack.push({
                            type: 'block-map',
                            offset: this.offset,
                            indent: this.indent,
                            items: [{ start: [this.sourceToken] }]
                        });
                    }
                    this.onKeyLine = true;
                    return;
                case 'map-value-ind':
                    if (includesToken(it.start, 'explicit-key-ind')) {
                        if (!it.sep) {
                            if (includesToken(it.start, 'newline')) {
                                Object.assign(it, { key: null, sep: [this.sourceToken] });
                            }
                            else {
                                const start = getFirstKeyStartProps(it.start);
                                this.stack.push({
                                    type: 'block-map',
                                    offset: this.offset,
                                    indent: this.indent,
                                    items: [{ start, key: null, sep: [this.sourceToken] }]
                                });
                            }
                        }
                        else if (it.value) {
                            map.items.push({ start: [], key: null, sep: [this.sourceToken] });
                        }
                        else if (includesToken(it.sep, 'map-value-ind')) {
                            this.stack.push({
                                type: 'block-map',
                                offset: this.offset,
                                indent: this.indent,
                                items: [{ start, key: null, sep: [this.sourceToken] }]
                            });
                        }
                        else if (isFlowToken(it.key) &&
                            !includesToken(it.sep, 'newline')) {
                            const start = getFirstKeyStartProps(it.start);
                            const key = it.key;
                            const sep = it.sep;
                            sep.push(this.sourceToken);
                            // @ts-expect-error type guard is wrong here
                            delete it.key, delete it.sep;
                            this.stack.push({
                                type: 'block-map',
                                offset: this.offset,
                                indent: this.indent,
                                items: [{ start, key, sep }]
                            });
                        }
                        else if (start.length > 0) {
                            // Not actually at next item
                            it.sep = it.sep.concat(start, this.sourceToken);
                        }
                        else {
                            it.sep.push(this.sourceToken);
                        }
                    }
                    else {
                        if (!it.sep) {
                            Object.assign(it, { key: null, sep: [this.sourceToken] });
                        }
                        else if (it.value || atNextItem) {
                            map.items.push({ start, key: null, sep: [this.sourceToken] });
                        }
                        else if (includesToken(it.sep, 'map-value-ind')) {
                            this.stack.push({
                                type: 'block-map',
                                offset: this.offset,
                                indent: this.indent,
                                items: [{ start: [], key: null, sep: [this.sourceToken] }]
                            });
                        }
                        else {
                            it.sep.push(this.sourceToken);
                        }
                    }
                    this.onKeyLine = true;
                    return;
                case 'alias':
                case 'scalar':
                case 'single-quoted-scalar':
                case 'double-quoted-scalar': {
                    const fs = this.flowScalar(this.type);
                    if (atNextItem || it.value) {
                        map.items.push({ start, key: fs, sep: [] });
                        this.onKeyLine = true;
                    }
                    else if (it.sep) {
                        this.stack.push(fs);
                    }
                    else {
                        Object.assign(it, { key: fs, sep: [] });
                        this.onKeyLine = true;
                    }
                    return;
                }
                default: {
                    const bv = this.startBlockValue(map);
                    if (bv) {
                        if (atNextItem &&
                            bv.type !== 'block-seq' &&
                            includesToken(it.start, 'explicit-key-ind')) {
                            map.items.push({ start });
                        }
                        this.stack.push(bv);
                        return;
                    }
                }
            }
        }
        yield* this.pop();
        yield* this.step();
    }
    *blockSequence(seq) {
        const it = seq.items[seq.items.length - 1];
        switch (this.type) {
            case 'newline':
                if (it.value) {
                    const end = 'end' in it.value ? it.value.end : undefined;
                    const last = Array.isArray(end) ? end[end.length - 1] : undefined;
                    if (last?.type === 'comment')
                        end?.push(this.sourceToken);
                    else
                        seq.items.push({ start: [this.sourceToken] });
                }
                else
                    it.start.push(this.sourceToken);
                return;
            case 'space':
            case 'comment':
                if (it.value)
                    seq.items.push({ start: [this.sourceToken] });
                else {
                    if (this.atIndentedComment(it.start, seq.indent)) {
                        const prev = seq.items[seq.items.length - 2];
                        const end = prev?.value?.end;
                        if (Array.isArray(end)) {
                            Array.prototype.push.apply(end, it.start);
                            end.push(this.sourceToken);
                            seq.items.pop();
                            return;
                        }
                    }
                    it.start.push(this.sourceToken);
                }
                return;
            case 'anchor':
            case 'tag':
                if (it.value || this.indent <= seq.indent)
                    break;
                it.start.push(this.sourceToken);
                return;
            case 'seq-item-ind':
                if (this.indent !== seq.indent)
                    break;
                if (it.value || includesToken(it.start, 'seq-item-ind'))
                    seq.items.push({ start: [this.sourceToken] });
                else
                    it.start.push(this.sourceToken);
                return;
        }
        if (this.indent > seq.indent) {
            const bv = this.startBlockValue(seq);
            if (bv) {
                this.stack.push(bv);
                return;
            }
        }
        yield* this.pop();
        yield* this.step();
    }
    *flowCollection(fc) {
        const it = fc.items[fc.items.length - 1];
        if (this.type === 'flow-error-end') {
            let top;
            do {
                yield* this.pop();
                top = this.peek(1);
            } while (top && top.type === 'flow-collection');
        }
        else if (fc.end.length === 0) {
            switch (this.type) {
                case 'comma':
                case 'explicit-key-ind':
                    if (!it || it.sep)
                        fc.items.push({ start: [this.sourceToken] });
                    else
                        it.start.push(this.sourceToken);
                    return;
                case 'map-value-ind':
                    if (!it || it.value)
                        fc.items.push({ start: [], key: null, sep: [this.sourceToken] });
                    else if (it.sep)
                        it.sep.push(this.sourceToken);
                    else
                        Object.assign(it, { key: null, sep: [this.sourceToken] });
                    return;
                case 'space':
                case 'comment':
                case 'newline':
                case 'anchor':
                case 'tag':
                    if (!it || it.value)
                        fc.items.push({ start: [this.sourceToken] });
                    else if (it.sep)
                        it.sep.push(this.sourceToken);
                    else
                        it.start.push(this.sourceToken);
                    return;
                case 'alias':
                case 'scalar':
                case 'single-quoteñûÿÿ‹EÌ3É‹}Ø3ÒB‰‹Ïèİÿÿ‹Ø…Û…ÓûÿÿèˆÙÿÿ‹Ø…Û…Äûÿÿ‹GM°‹@@  PèF  ‹EÔ‹MÈ‰EÌ‰MÄM ‹ ‰E¼‹FƒÀ‰E¸E¸‰E EÄ‰E¤E¼‰E¨EÌ‰E¬èr ‹Øé]ûÿÿ‹EÌ3Ò‹MØ3ÿB‰8è†Üÿÿ‹Ø…Û…Uûÿÿ‹}È‹EÔjZ…ÿ„ÿÿÿ9‚ÿÿÿèïØÿÿ‹Ø…Û…+ûÿÿ‹EØM°‹@‹@@  Pèª  ‹NUÀè@l  é®şÿÿ‹EÌ3É‹}Ø3ÒB‰‹ÏèÜÿÿ‹Ø…Û…æúÿÿè›Øÿÿ‹Ø…Û…×úÿÿ‹GM°‹@@  PèY  ‹MÈ‹}Ô…Ét-‹F‹  9r Rÿ°  QèA… ‹FƒÄ‹€  ‰éwúÿÿ‹F‹€  ‰‹}Ø3À…É„dúÿÿ»ê   éZúÿÿ‹EÌ3É‹}Ø3ÒB‰‹ÏèÛÿÿ‹Ø…Û…LúÿÿèØÿÿ‹Ø…Û…=úÿÿ‹GM°‹@@  Pè¿  ‹VƒÂ‹ÊA‰E¸f‹ƒÁf;E¼uô+M¸3ÀPPPPÑùAPRh   3ÀPÿø0‰EÄ…ÀuÿH0éişÿÿ‹MÈ‹}Ô…É„Nÿÿÿ9‚Fÿÿÿ‹vƒÆ‹ÖB‰E¸f‹ƒÂf;E¼uô+U¸3ÀPPÿuÄÑúQBPVh   3ÀPÿø0…À…ôşÿÿéïúÿÿÌÌÌÌÌÌÌÌÌÌÌÌÌ‹ÿU‹ìV3Ò3öèÒÆÿÿ¡  =  t%ö@t€xrÿ5Ğ*º” ÿpÿpj3YèŞÇÿÿƒ}‹E WtÇ    ëH‰0‹E‹@…Àt790t3‹}‹P‹M9r…Ét‰ÿpÿ0Qè—ƒ ƒÄë‹ñ‰÷Şöæê   ë¾
 	€3ÒBè?Æÿÿ¡  _=  t&ö@t €xrVÿ5Ğ*º” ÿpÿpj4Yè“Çÿÿ‹Æ^]Â ‹ÿU‹ìQQ‹ESVWƒÀ‰UüP‹Ù‰Eø3ÿèäÈÿÿ‹ğ…öuFjZè×Åÿÿj_¡  =  „Š   ö@„€   €xrzÿ5Ğ*º” ÿpÿpj5YèØÆÿÿë_‹Eü‰F‹EPÿu‰FFPÇ  è°‚ ƒÄCP‹CƒàP‹WÿuøVÿ0ÿXP…Àu
ÿH0‹øë‹Eü‰C‹EÁà‰CVèYÈÿÿ‹Ç_^[ÉÂ j@¸øèş€ ‹]3É‹Á‰Mä‰Eì‹ñ‰EÜ‹‰MàÇEĞ ‰MÔ‹@‰Eè‰Mü}´jY3À3Òó«èêÄÿÿ¡  =  t%ö@t€xrÿ5Ğ*º” ÿpÿpj6YèöÅÿÿ‹}¶ƒè„Ì  ƒè„“  ƒè…¬  ‹EMàƒ  è[Íÿÿ‹ğ…öuZ9E„  ÿuèrÇÿÿ‹ğ‰uì…ö…§   jZè^Äÿÿj^¡  =  t%ö@t€xrÿ5Ğ*º” ÿpÿpj8YègÅÿÿ‹}ìƒ}à tMàè*Íÿÿ…ÿtWè.Çÿÿ3ÒBèÄÿÿ¡  =  t&ö@t €xrVÿ5Ğ*º” ÿpÿpj9Yè\ÅÿÿƒMüÿMĞè  ‹Æèi Â ÿuWVèë€ ‹EƒÄƒÀø‰EäEäPFP‹Ej jj ÿpÿdP…ÀuÿH0‹ğéTÿÿÿ‰uÜë‰}Ü $  t' ¤  t
¾ 	€é0ÿÿÿ‹Eà3Ò9Ptu¾
 	€éÿÿÿ‹Eà3Ò9PptìöCt
¾	 	€éÿÿÿ‹÷A   ğt
¾ 	€éíşÿÿ‹Eè‹@9p  t
¾4 €éÕşÿÿ9x  u
¾" €éÃşÿÿ9Qu
¾0 €é´şÿÿè'Óÿÿ‹ğ…ö…¥şÿÿ‹MĞ‹@‹@@  Pèã  ‹3ÒVBè8Õÿÿ‹ğ…ö…P  ‹EÜPÿp3À ¤  •À@P‹EèjŠP‹Hè1<  ‹ğ…ö…!   ¤  u	ÇC   ë $  uÇC   ‹EÜMó‹QQ‹@ÿs‰C‹RP‹Eè‹Hè(c  ‹ğ…ö…Ó  ‹C3Òƒ{P”Âèßÿÿé¸   ¤  u	ÇC   ë $  uÇC   ‹EÇ    éÏıÿÿ‹Eƒ  ‹Eƒxu
¾
 	€é¶ıÿÿ ¤  t
¾ 	€é£ıÿÿ‹9t
¾ 	€é“ıÿÿ÷A   ğt
¾ 	€é€ıÿÿèØÑÿÿ‹ğ…ö…qıÿÿ‹MĞ‹@‹@@  Pè”  ‹Eè3Ò‹j‹@‹@‹ ‰EØèÛÓÿÿ‹ğ…ö…ó   ‹E3Ò‹MèƒÀô‰EÄBG‰U¼‹}Ø‰EÀŠAˆE¸ƒÿrjX‰E´‰EÌë‰U´‹IU´èO  ‹ğ…ö…ª   ƒÿrrÿuÄèğÃÿÿ‹ø‰}ì…ÿu>jZèàÀÿÿj^¡  =  t}ö@tw€xrqÿ5Ğ*º” ÿpÿpj7YèéÁÿÿëV‹EÄ3ÉPÿ…Àt‹EÀŠˆA‹EÄJ;Èrî‰Eäë"‹UÄEä‹MÀPEÜPèç  ‹}Ü‹ğ…öu‹Eä‰}ì‹U‹ËPW‹RèQúÿÿ‹ğ‹}ìƒ}Ô „ üÿÿ‹ƒeÔ èíÑÿÿéüÿÿÌÌÌÌÌÌ‹ÿU‹ìQQSV3ö3ÒW‰uü‰uøèÀÿÿ¡  =  t%ö@t€xrÿ5Ğ*º” ÿpÿpj:Yè$Áÿÿ‹M…Étp9utk9utfƒ9t¿  ëgƒyuO9quJ‹QEü‹IPEøPè  ‹ø…ÿu,‹Uü‹]øƒêtŠŠ3ˆ3FˆJ;òrî‹E‹Mü‰‹E‰ë‹uøëjW_ëjW_…ötVè‚Âÿÿ3ÒBè[¿ÿÿ¡  =  t&ö@t €xrWÿ5Ğ*º” ÿpÿpj;Yè°Àÿÿ‹Ç_^[ÉÂ ÌÌÌÌÌÌÌÌÌÌÌÌÌÌÌj<¸èŞz ‹M3À‰Eè3Û!]Ø3ö‰Eğ‹‰]Ü‰]ìÇEÔ ‹@‰Eä!]ü3ÒèÔ¾ÿÿ¡  =  t%ö@t€xrÿ5Ğ*º” ÿpÿpj<Yèà¿ÿÿj3À}¸Yó«‹}3É‹E A9O…$  ƒ  ‹÷A   ğt
¾ 	€é>  è¸Îÿÿ‹ğ…ö…/  ‹MÔ‹@‹@@  Pèt  ‹Eä3Ò‹j‹@‹@‹ ‰Eàè»Ğÿÿ‹ğ…ö…Æ   ‹}3Ò‹MäBƒ}à‰UÀ‹‰EÈ‹E‰EÄŠAˆE¼rjX‰E¸‰EĞë‰U¸‹IU¸èøK  ‹ğ…ö…€   ƒ}àr5!Eà‹EÈxÿ…ÿt!‹]à‹MÄŠ9ŠˆC‹EÄˆ8O;ßrè‹EÈ‹]Ü‹M‰ëE‹UÈEì‹MÄPEğPè  ‹]ì‹ğ…öu"9‹}ğs¾ 	€ëSWÿuè¨z ‹MƒÄ‰ë
‹}ğë‰‹}èƒ}Ø t‹MƒeØ ‹	èûÎÿÿ…ÿt‹Ç…Ût	Æ  @ƒëu÷WèPÀÿÿ3ÒBè)½ÿÿ¡  =  t&ö@t €xrVÿ5Ğ*º” ÿpÿpj=Yè~¾ÿÿƒMüÿMÔè:  ‹Æè‹x Â jX¸>è®x ‹]ÇEä   ÇEÈ ‹‹@‰EÔ3À‹ğ‰Eì‰Eà‰EØ‰EÜ‰Eè‰EÌ3Ò‰Eüèœ¼ÿÿ¡  =  t%ö@t€xrÿ5Ğ*º” ÿpÿpj>Yè¨½ÿÿj,Eœj Pèšy ‹EƒÄƒ  ÷Eşÿÿÿt
¿	 	€éO  ‹÷A   ğt
¿ 	€é:  èvÌÿÿ‹ø…ÿ…+  ‹MÈ‹@‹@@  Pè2  ‹UEàPQ‹è¦Èÿÿ‹ø…ÿ…ë  ‹M‹Uà‹E‰UĞ9‚p  …À„h  WEìPWjÿsÿTP…ÀuÿH0‹øé­  ÿuìè¨¾ÿÿ‹ğ‰uğ…öuOjZè˜»ÿÿj_‹  ù  „  öA„u  €y‚k  ÿ5Ğ*º” ÿqÿqj?Yè“¼ÿÿéM  j EìPVjÿsÿTP…À„yÿÿÿj EÜÇEÜ   PEØPjÿsÿTP…À„Uÿÿÿ‹Eäƒøu>ÿu‹3Òè‰Íÿÿ‹ø…ÿ…ñ   ‹Eì‰E´‹EØ‰E¬‹E‰E¨‹E‰E¤‹EÔÇEœ   Š@ˆE ‹EÔMœ‰u°Q‹@‹@P‹°˜   ‹Îÿ02ÿÖ‹ø…ÿ…›   ‹uäƒşu‹Eè3Òƒ}P”Âè‡×ÿÿ‹ø…ÿuyÿuj ÿuèÿuĞÿu¸ÿsÿ`P…ÀuÿH0‹ø…ÿt;ÿ 	€uJƒşuE‹M¼‹E¸…ÉtÆ  @ƒéu÷‹E¸PèO½ÿÿƒe¸ ‹uğjX‰Eäéÿÿÿ‹u¼Vÿu¸ÿuèEw ‹EƒÄ‰0‹uğƒ}Ì t‹ƒeÌ è¤Ëÿÿ‹E¸…Àt‹M¼…Ét	Æ  @ƒéu÷ÿu¸èó¼ÿÿ…ötVèé¼ÿÿƒ}è t	ÿuèÿhP3ÒBè³¹ÿÿ¡  =  t&ö@t €xrWÿ5Ğ*º” ÿpÿpj@Yè»ÿÿƒMüÿMÈèÄ  ‹Çèu Â ‰…À„]ÿÿÿ¿ê   éSÿÿÿÌÌÌÌÌÌ‹ÿU‹ìSVW3Ò3öè@¹ÿÿ¡  ¿  »” ;Çt"ö@t€xrÿ5Ğ*‹ÓÿpÿpjAYèHºÿÿ‹E‰0‹E9pu¾ 	€ë%ÿuVÿp‹Eÿuÿuÿpÿ`P…ÀuÿH0‹ğ3ÒBèÊ¸ÿÿ‹  ;Ït#öAt€yrVÿ5Ğ*‹ÓÿqÿqjBYè$ºÿÿ_‹Æ^[]Â ÌÌÌj¸aè^t 3ÛÇEè ‹ó‰]ğ‰]ì3Ò‰]üèj¸ÿÿ¡  =  t%ö@t€xrÿ5Ğ*º” ÿpÿpjCYèv¹ÿÿ‹}ƒtƒt‹EÇ    é…   ‹E3ÒB‰‹èÊËÿÿ‹ğ…öupèPÈÿÿ‹ğ…öue‹Mè‹@‹@@  Pè  ‹WEğPQ‹è„Äÿÿ‹ğ…öu*‹Eğ…Àt‹3ÒÁàƒ‰GGP”Âè±Ôÿÿ‹ğë¾ 	€ƒ}ì t
‹‰]ìèHÉÿÿ3ÒBè·ÿÿ¡  =  t&ö@t €xrVÿ5Ğ*º” ÿpÿpjDYèä¸ÿÿƒMüÿMèè   ‹Æèñr Â ÌÌÌÌÌÌ‹ÿU‹ìƒäøQ‹ES3ÛV‹óW+Ã„›   ƒè…(  QQèŞe ‹}‰=&…ÿ„Ê   jºØ!‰=¬.‹Ïè# …À…®   ‹ÏÇØ+   è­¾ÿÿ…À…•   ÇÔ*   ‰°*‰ *Ç¸*   ‰¼*Çœ*¤ Ç   *èD¹ÿÿFé–   ¡ì-…ÀtPÿ|0‰ì-‹5  ¿  ;÷t(ë‹FFtÿvÿvÿ|1‰^‰^‹6…öuà‰=  3öF9Ø+tjZ¹Ø!èı ‰Ø+9Ô*t#¡°.…ÀtPè(¹ÿÿ3É‰°.Aè¼ÿÿ‰Ô*_‹Æ^[‹å]Â ÌÌÌÌÌÌÌÌÌ‹Iè °Ã‹ÿU‹ìV‹ñW‹}‹N;ùt…Étèï ‰~_^]Â ‹ÿV‹ñ‹NÇ …Ét	èÏ ƒf ^Ã‹ÿU‹ì3À…Òtúÿÿÿv¸W €…ÀxQÿuQè¼   ë…ÒtÆ ]Â ‹ÿU‹ìº   ¸à.‹Ê€8 t@ƒéuõV‹Â‹ñ+Á÷Şö#ğ‹Á÷ØÀ%©ÿøW €…ÉtQÿu+ÖQà.è`   ^]Â ‹ÿU‹ì‹EVÑè‹ğ÷Şöæ©ÿøÆW €…Àt5S‹]3öWxÿEPÿuWSÿÈ1ƒÄ…Àx;Çwuë¾z €3Àf‰{_[‹Æ^]Ã‹ÿU‹ì…Òt&‹ESV¾şÿÿ+Á…ötŠ„Ût	ˆANƒêuì^[…ÒuI÷ÚÆ Òâ†ÿø‚z €]Â ‹ÿU‹ìQQSVW‰Uüÿ¸0‹5h"3Û‹øƒşÿu‹ó‰=è,‰5h"‰ì,ëbƒşşt	9<õè,tPƒÈÿ‹Ë‹Íè,;×tƒøÿu…Òu‹ÁAƒù räë‹ñ‰5h"ƒù u ƒøÿt‰<Åè,‹ğ‰Åì,ëjş^‰5h"…öxƒ}ü uÿõì,‹<õì,ë‹û¡  3Éˆà.Aö@t!€xr;ù|‹Ùhø è.şÿÿ…ÀuC;ß~í3Û‹Mü‹Á+Ãtƒètƒèuh  ëh ëhü èøıÿÿ‹Mü…öxƒùu)õì,u‰õè,_^Çà,à.[ÉÃ‹ÿU‹ì;Ár‹ĞëƒÊÿ;Á‹MÀ%€‰]Â ƒ=ˆ& ujYÍ)3ÀÃ‹ÿU‹ìQ¡ä,V3ö‰uüW‹ù…ÀuIh   ğÿuEüRVPÿœP…Àu
ÿH0‹ğë)‹Müºä,3Àğ±
¡ä,;EütVÿuüÿlP¡ä,‰_‹Æ^ÉÂ ‹ÿU‹ìQ‹Á‹MSV3ö‰Eü‰1‹MW‰1ƒú‚™   €|ş…   €|ÿ …ƒ   zı…ÿt|€< tƒïuõ…ÿtm